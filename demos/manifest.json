{
  "_comment": "Registry of every mobile-cc demo. The single source of truth — agents add/update an entry here when they ship a feature. See demos/CONVENTIONS.md. `media` is the docs/media/<media>.{mp4,gif,png} basename a demo publishes; omit (null) for test-only workflows not embedded anywhere. `attachment_url` is the GitHub user-attachments player URL embedded in README.md (GitHub strips <video> tags, so mp4s are embedded via that URL). `recapture_pending` downgrades the resolution gate to a warning for a known-stale asset.",
  "min_video_width": 640,
  "demos": [
    {
      "id": "use-flow",
      "kind": "ui",
      "title": "Drive a live Claude Code session",
      "description": "The real Claude Code TUI rendered live in the browser — project-grouped tabs (api ×3 + docs), quick-keys row, a reply composed in the Message box. The README 'See it' clip.",
      "media": "use",
      "profile": "multi-project",
      "readme_section": "See it",
      "attachment_url": "https://github.com/user-attachments/assets/e580857a-fc23-4d0b-92e2-d2fa0180a3c0"
    },
    {
      "id": "paste-flow",
      "kind": "ui",
      "title": "Image paste end-to-end",
      "description": "Paste a screenshot into the Message box -> uploads to the daemon -> Claude Code's vision pipeline reads the [image:/abs/path]. Regression test only for now (not embedded in README).",
      "media": null
    },
    {
      "id": "install",
      "kind": "terminal",
      "title": "Install via curl | bash",
      "description": "The `curl ... | bash` install flow: download from GitHub Releases, verify (minisign/provenance/sha256), drop the systemd user unit, print the URL.",
      "media": "install",
      "readme_section": "2. Install mobile-cc",
      "attachment_url": "https://github.com/user-attachments/assets/e38891e6-946f-41db-b7de-c65f66f2a6b2"
    },
    {
      "id": "brew-install",
      "kind": "terminal",
      "title": "Install via Homebrew",
      "description": "`brew install eyalev/tap/mobile-cc` — the tap install path (binary only, no systemd unit).",
      "media": "brew-install",
      "readme_section": "2. Install mobile-cc",
      "attachment_url": "https://github.com/user-attachments/assets/af1e0d4e-0d51-4584-9abe-457793d43678"
    }
  ]
}
